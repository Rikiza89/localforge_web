# LocalForge 完全仕組み解説
## — フォルダを開いてからQ&Aまで、裏側で何が起きているのか —

> **対象読者:** IT初心者の方。専門用語は必ずたとえ話と一緒に説明します。

---

## 目次

1. [LocalForgeとは何か](#1-localforgeとは何か)
2. [アプリの構成要素](#2-アプリの構成要素)
3. [プロジェクトフォルダを開く](#3-プロジェクトフォルダを開く)
4. [インデックスとは何か](#4-インデックスとは何か)
5. [インデックス構築の詳細な手順](#5-インデックス構築の詳細な手順)
6. [埋め込み（Embedding）とベクトル検索](#6-埋め込みembeddingとベクトル検索)
7. [Q&Aの仕組み — 質問から回答まで](#7-qaの仕組み--質問から回答まで)
8. [コード生成（Generate）の仕組み](#8-コード生成generateの仕組み)
9. [レポート生成（Explain）の仕組み](#9-レポート生成explainの仕組み)
10. [その他の重要な仕組み](#10-その他の重要な仕組み)
11. [全体のデータフロー図](#11-全体のデータフロー図)

---

## 1. LocalForgeとは何か

LocalForgeは「**ローカルで動くAIコードアシスタント**」です。

### たとえ話

想像してください。あなたには非常に優秀な助手がいます。この助手は：
- あなたのプロジェクトフォルダの**全ファイルを読んで記憶**できる
- 「このファイルは何をしているの？」という質問に**具体的に答えられる**
- 「新しい機能を作って」と言うと**コードを書いてくれる**
- すべての処理が**あなたのPC内だけ**で完結する（インターネット不要）

LocalForgeはまさにこの助手の役割を果たします。そして、AIとしての頭脳は **Ollama**（後述）が担います。

### 3つのモード

```
┌─────────────────────────────────────────────────────┐
│                   LocalForge                        │
├─────────────┬──────────────────┬────────────────────┤
│  Generate   │     Resume       │     Explain        │
│  （生成）   │   （再開・改善） │   （分析・Q&A）    │
├─────────────┼──────────────────┼────────────────────┤
│ 新しいプロ  │ 既存プロジェクト │ コードを分析して   │
│ ジェクトを  │ を引き継いで改善 │ レポートや質問回答 │
│ ゼロから作成│                  │                    │
└─────────────┴──────────────────┴────────────────────┘
```

---

## 2. アプリの構成要素

LocalForgeは複数の部品が連携して動いています。まずそれぞれを理解しましょう。

### 2-1. Flask（フラスク）— アプリのサーバー部分

**Flaskとは:** PythonでWebサーバーを作るためのツールです。

**たとえ話:** レストランで言うと「厨房」です。お客さん（ブラウザ画面）から注文が来たら、料理（データ処理）を行って結果を返します。

LocalForgeのFlaskサーバーは `127.0.0.1:7331` というアドレスで動いています。`127.0.0.1` は「自分のPC」を意味し、外部からはアクセスできません。

### 2-2. pywebview — 画面表示部分

**pywebviewとは:** Pythonアプリをネイティブのデスクトップウィンドウとして表示するツールです。

**たとえ話:** Webブラウザを「額縁」に入れて、デスクトップアプリのように見せる仕組みです。中身はWebページですが、見た目はアプリです。

### 2-3. Ollama（オラマ）— AIの頭脳

**Ollamaとは:** PC上でAI言語モデル（LLM）を動かすためのツールです。

**LLMとは（Language Model / 言語モデル）:** 大量のテキストから学習して、人間のような文章を生成できるAIです。ChatGPTと同じ種類のAIですが、すべてローカルで動きます。

**たとえ話:** Ollamaは「AI専用の実行エンジン」です。モデルファイル（AIの頭脳データ）をロードして、LocalForgeからの質問に答えます。

```
┌─────────────────────────────────────────────┐
│              LocalForge全体像               │
│                                             │
│  ┌──────────┐    ┌──────────────────────┐  │
│  │ ブラウザ  │◄──►│  Flask サーバー      │  │
│  │ (画面UI) │    │  (port 7331)         │  │
│  └──────────┘    └──────────┬───────────┘  │
│                              │              │
│                    ┌─────────▼──────────┐   │
│                    │  アプリケーション  │   │
│                    │  ロジック（Python）│   │
│                    └─────────┬──────────┘   │
│                              │              │
│              ┌───────────────┼──────────┐   │
│              │               │          │   │
│        ┌─────▼────┐  ┌───────▼──┐ ┌────▼─┐ │
│        │  ファイル │  │  Ollama  │ │ DB   │ │
│        │  システム │  │ (AI脳)   │ │(索引)│ │
│        │  (ディスク│  │port 11434│ │      │ │
│        └──────────┘  └──────────┘ └──────┘ │
└─────────────────────────────────────────────┘
```

---

## 3. プロジェクトフォルダを開く

ユーザーがフォルダを選択すると、裏側で以下が起きます。

### ステップ1: フォルダ情報の取得

```
ユーザーがフォルダを選択
         │
         ▼
Flask: POST /api/project/open
         │
         ▼
Python: フォルダ内の全ファイルをスキャン
         │
         ▼
ファイルツリーを構築（階層構造）
         │
         ▼
ブラウザに送信 → 画面左のツリーに表示
```

**ファイルツリーとは:** フォルダの中のファイルとフォルダを木の枝のように表現したものです。

```
myproject/
├── src/
│   ├── main.py
│   └── utils.py
├── docs/
│   └── README.md
└── requirements.txt
```

### ステップ2: 設定ファイルの確認

LocalForgeは各プロジェクトの設定を `.localforge/config.json` に保存します。このフォルダがあるかどうかで、そのプロジェクトを以前に使ったことがあるかを判断します。

```
フォルダを開く
    │
    ├── .localforge/があるか？
    │       │
    │   YES │               NO
    │       ▼               ▼
    │  既存設定を          新規プロジェクトとして
    │  読み込む            初期設定を作成
    │       │               │
    │       └───────┬───────┘
    │               ▼
    │    モード判定（Generate/Resume/Explain）
```

### ステップ3: モード自動判定

LocalForgeはフォルダの内容を見て、どのモードで開くべきかを自動判断します。

- **Explainモード:** フォルダにすでにコードがたくさんある（= 既存プロジェクトの分析）
- **Generateモード:** フォルダが空または新規
- **Resumeモード:** LocalForgeが以前に生成したプロジェクト（生成ログが存在する）

---

## 4. インデックスとは何か

### 4-1. インデックスの概念（IT基礎知識）

**インデックス（索引）** は、情報を素早く見つけるための「目次」や「索引」です。

**たとえ話1 — 図書館:**
図書館に本が10万冊あるとします。「人工知能について書いた本」を探すとき、全部の本を1冊ずつ読むのは不可能です。でも「テーマ別カード索引」があれば、すぐに見つかります。コンピュータのインデックスも同じです。

**たとえ話2 — 本の巻末索引:**
本の最後にある「索引」ページを思い出してください。「機械学習 → 43, 78, 120ページ」のように、キーワードと場所が対応しています。LocalForgeのインデックスも同じで、「このファイルにはどんな内容があるか」を記録します。

### 4-2. LocalForgeのインデックスとは

LocalForgeは各ファイルについて以下の情報を記録します：

```
FileChunk（ファイルの記録単位）
┌──────────────────────────────────────────┐
│ path        : ファイルのパス             │
│ summary     : AIが生成した要約文         │
│ language    : プログラミング言語         │
│ imports     : このファイルが使う他ファイル│
│ mtime       : 最終更新時刻               │
│ size        : ファイルサイズ             │
│ content     : ファイルの内容（一部）     │
└──────────────────────────────────────────┘
```

### 4-3. インデックスの保存場所

```
プロジェクトフォルダ/
└── .localforge/
    ├── index.jsonl          ← 各ファイルの記録（1行=1ファイル）
    ├── project_index.json   ← プロジェクト全体の要約
    ├── config.json          ← 設定（使用モデルなど）
    ├── chroma/              ← ベクトルデータベース（後述）
    └── cache/               ← キャッシュ（高速化のための保存）
```

**JSONL（ジェイソンライン）とは:** 1行に1つのデータを記録するファイル形式です。大量のデータを効率的に追記・読み込みできます。

---

## 5. インデックス構築の詳細な手順

ユーザーが「インデックスを構築」ボタンを押すと、以下が順に実行されます。

```
インデックス構築開始
        │
        ▼
Step 1: プロジェクト内の全ファイルをリストアップ
        │
        ▼
Step 2: 各ファイルを読み込む
        │
        ├── 前回から変更なし？ → スキップ（時間節約）
        │
        └── 変更あり or 新規？ → 処理する
              │
              ▼
Step 3: ファイルの種類を判断
        │
        ├── シンプルなファイル（設定ファイル、短いコードなど）
        │         → ルールベースで自動要約（AIなし）
        │
        └── 複雑なコードファイル
                  → LLM（AI）に要約を依頼
                        │
                        ▼
Step 4: 要約をインデックスに保存（.localforge/index.jsonl）
        │
        ▼
Step 5: 埋め込み（Embedding）の生成 ← 次の章で詳しく説明
        │
        ▼
Step 6: プロジェクト全体の要約を生成
        │
        ▼
Step 7: 完了
```

### 5-1. 差分更新（インクリメンタル更新）

毎回全ファイルを処理するのは時間がかかります。LocalForgeは**ファイルの更新日時とサイズ**をチェックして、変更されたファイルだけを再処理します。

**たとえ話:** 100ページの本を改訂するとき、全部を書き直すのではなく、変わったページだけ差し替えるイメージです。

### 5-2. LLMバッチ処理

複数のファイルをまとめてAIに送ることで、効率化しています。

**たとえ話:** 荷物を1個ずつ配送するより、まとめてトラック1台で運ぶ方が効率的です。

### 5-3. 依存関係の解析（import解析）

Pythonの `import` 文やJavaScriptの `require` 文を解析して、ファイル同士のつながりを記録します。

```python
# main.py
from utils import helper_function   ← これを検出して記録
import os                           ← 標準ライブラリは無視
```

```
main.py ──imports──► utils.py
utils.py ──imports──► config.py
```

このつながりの情報が、後のQ&Aで「関連ファイルを自動で取得する」機能に使われます。

---

## 6. 埋め込み（Embedding）とベクトル検索

ここは少し難しい概念ですが、たとえ話でわかりやすく説明します。

### 6-1. 埋め込み（Embedding）とは

**Embeddingとは:** テキスト（文章）を数字の列（ベクトル）に変換する技術です。

**たとえ話 — 地図上の座標:**
「東京」という言葉を地図上の座標(35.6, 139.7)で表せるとします。「大阪」は(34.7, 135.5)です。この座標を使うと「東京と大阪は近い（日本の都市）」「東京とパリは遠い（別の国）」ということが計算でわかります。

Embeddingも同じです。「ファイル読み込み」というテキストと「ファイルを開く」というテキストは意味が似ているので、数字の座標が近くなります。

```
「ファイル読み込み」  → [0.23, 0.87, 0.12, 0.45, ...]  (384個の数字)
「ファイルを開く」    → [0.21, 0.89, 0.11, 0.43, ...]  (似た数字！)
「データベース設計」  → [0.91, 0.02, 0.78, 0.33, ...]  (全く違う数字)
```

### 6-2. ベクトルとは

**ベクトルとは:** 複数の数字の組み合わせです。中学数学で習った「方向と大きさを持つ矢印」のことです。コンピュータの世界では、単純に「数字のリスト」と考えてください。

```
2次元ベクトル（わかりやすい例）:
「犬」    → (0.8, 0.2)  ← かわいい=高、危険=低
「虎」    → (0.3, 0.9)  ← かわいい=低、危険=高
「猫」    → (0.7, 0.15) ← かわいい=高、危険=低（犬に近い！）
```

実際のEmbeddingは384次元（384個の数字）ですが、原理は同じです。

### 6-3. ChromaDB — ベクトルデータベース

**ChromaDB（クロマDB）とは:** ベクトルを高速に保存・検索するためのデータベースです。

**たとえ話:** 普通のデータベースは「名前で検索」できますが、ChromaDBは「意味の近さで検索」できます。「これに似た内容のファイルを探して」という検索が可能です。

LocalForgeでは：
1. インデックス構築時に各ファイルの要約をEmbeddingに変換
2. ChromaDBに保存（`.localforge/chroma/` フォルダ）
3. Q&A時に「質問のEmbedding」と「ファイルのEmbedding」を比較
4. 意味的に近いファイルを自動選択

```
質問「エラーハンドリングはどこで実装されている？」
         │
         ▼
質問をEmbeddingに変換 → [0.34, 0.71, ...]
         │
         ▼
ChromaDBに「似たベクトルを検索」と依頼
         │
         ▼
最も近いファイルを返す:
  1. error_handler.py  （類似度: 0.95）
  2. exceptions.py     （類似度: 0.88）
  3. middleware.py     （類似度: 0.72）
```

### 6-4. BM25 — キーワード検索（フォールバック）

ChromaDBやEmbeddingが使えない場合、LocalForgeは **BM25** というキーワード検索に切り替えます。

**BM25（フォールバック）とは:** Googleの検索のようなキーワードマッチング技術です。「この単語が含まれるファイル」を探します。

**フォールバックとは:** メインの機能が使えないときに自動的に切り替わる代替機能のことです。

### 6-5. RAG（Retrieval Augmented Generation）

**RAGとは:** 「検索して取得した情報をもとにAIが回答を生成する」技術です。

**たとえ話:** 試験で「教科書を見ながら答えてよい」というオープンブック試験を想像してください。AIだけに「知ってる？」と聞くのではなく、まず関連する情報を検索して見つけてから、それをもとに回答を作ります。

```
RAGの流れ:

質問 → 関連ファイルを検索 → ファイル内容をAIに渡す → 回答生成
                ↑                        ↑
         （Retrieval:検索）     （Augmented:補強）
                                （Generation:生成）
```

---

## 7. Q&Aの仕組み — 質問から回答まで

これがLocalForgeの最も複雑な機能です。8つのフェーズに分けて説明します。

### 全体フロー

```
ユーザーが質問を入力
         │
         ▼
【Phase A】ピン留めファイルの解決
         │
         ▼
【Phase B】ワークスペース検索（複数プロジェクトある場合）
         │
         ▼
【Phase C/D】セマンティック検索でファイル選択
         │
         ▼
【Phase E】依存関係展開（BFS）
         │
         ▼
【Phase F】ファイル内容の読み込み
         │
         ▼
【Phase G】プロンプト構築
         │
         ▼
Ollamaに送信 → トークンを受信 → 画面に表示
```

### Phase A: ピン留めファイルの解決

**ピン留め（Pin）とは:** ユーザーが「このファイルは特に重要」と指定したファイルのことです。

ユーザーがピン留めしたファイルは必ずコンテキストに含まれます。さらに、そのファイルが使っている他のファイル（import先）も自動的に追加されます。

```
ユーザーがピン留め: [main.py, config.py]
         │
         ▼
main.py の import先を調べる
  → utils.py, database.py を発見 → 追加（深さ1）
         │
         ▼
utils.py の import先を調べる
  → helper.py を発見 → 追加（深さ2）
         │
         ▼
... 最大10段階まで繰り返す
         │
         ▼
最終的に集まったファイル全部をコンテキストに使用
```

**深さ（depth）によって読む量を調整:**

```
深さ0〜1（直接指定 or 直接import）: ファイル全体を読む
深さ2〜4（間接的なimport）        : 先頭6000文字だけ読む
深さ5〜10（遠いimport）           : 先頭1500文字だけ読む
```

**たとえ話:** 本を調べるとき、直接関係する章は全部読むが、参考文献は概要だけ、その参考文献の参考文献はタイトルだけ読む、というイメージです。

### Phase B: ワークスペース検索

複数のプロジェクトをワークスペースとして登録している場合、関連するプロジェクトからも情報を取得します。

### Phase C/D: セマンティック検索

ピン留めがない場合や、ピン留めに加えて追加情報が必要な場合、ChromaDBを使ってセマンティック検索（意味検索）を行います。

```
質問「データベース接続はどこ?」
         │
         ▼
質問をEmbedding変換 → ChromaDBで検索
         │
         ▼
最も関連するファイルを最大10個選択
```

### Phase E: BFS（幅優先探索）による依存関係展開

**BFS（Breadth-First Search / 幅優先探索）とは:** グラフ（ネットワーク）を広く探索するアルゴリズムです。

**たとえ話 — 友人のネットワーク:**
あなたの友人（深さ1）、友人の友人（深さ2）、そのまた友人（深さ3）... と広がっていくイメージです。

```
選択されたファイル（起点）
         │
    ┌────┴────┐
    │         │
 file_a    file_b      ← 深さ1（起点のimport先）
    │         │
 ┌──┴──┐  ┌──┴──┐
file_c  file_d  file_e  ← 深さ2（さらにimport先）
```

最大10段階まで展開し、最大200ファイルまで収集します。

### Phase F: ファイル内容の読み込み

収集したファイルの内容を実際に読み込みます。深さに応じて読む量を調整し、トークン予算内に収まるようにします。

**トークン（Token）とは:** AIが読む「テキストの単位」です。日本語の1文字≒1〜2トークン、英語の1単語≒1トークン程度です。AIには一度に処理できるトークン数の上限（コンテキストウィンドウ）があります。

**コンテキストウィンドウとは:** AIが一度に見られる情報の量です。本のページ数で言えば、AIは一度に「最大N枚のページ」しか見られない制限があります。

### Phase G: プロンプト構築

**プロンプト（Prompt）とは:** AIに渡す「質問文＋背景情報の全体」のことです。

LocalForgeはプロンプトを以下の順序で組み立てます：

```
┌──────────────────────────────────────────────┐
│ プロンプトの構造                              │
├──────────────────────────────────────────────┤
│ 1. プロジェクト概要（project_index.json）    │
│ 2. ファイル依存関係マップ                    │
│ 3. 関連ファイルのサマリー（最大8件）         │
│ 4. ワークスペース情報                        │
│ 5. 会話履歴（直近10件）                      │
│ 6. ピン留めファイルの内容（優先）            │
│    └ [ラベル付き]                            │
│      [USER-PINNED] main.py                   │
│      [AUTO-INCLUDED: direct import] utils.py │
│      [DOCUMENTATION - GROUND TRUTH] spec.md │
│ 7. その他ファイルの内容                      │
│ 8. 参照可能パス一覧（AUTHORIZED FILE PATHS） │
│ 9. 厳守ルール（5つ）+ 質問                  │
└──────────────────────────────────────────────┘
```

**厳守ルールの内容:**
- **RULE 1 (PATH LOCK):** 存在するパスのみ使用、でたらめなパス禁止
- **RULE 2 (FACT vs PROPOSAL):** 事実と提案を明確に区別する
- **RULE 3 (DOCUMENTATION IS AUTHORITATIVE):** ドキュメントファイルは最優先の真実
- **RULE 4 (CITE SOURCES):** 引用時は必ずファイルパスを明記
- **RULE 5 (DEPENDENCY FIDELITY):** import関係は実際のものだけ記述

### Ollamaへの送信とストリーミング

**ストリーミング（Streaming）とは:** 回答を一度に全部受け取るのではなく、生成されたトークン（単語・文字）を1つずつリアルタイムで受け取る方式です。

**たとえ話:** 印刷が終わるのを待って全部読むのではなく、印刷されながらリアルタイムで読み始めるイメージです。

```
LocalForge → Ollama: 「このプロンプトに答えて」
Ollama → LocalForge: 「こ」
Ollama → LocalForge: 「の」
Ollama → LocalForge: 「ファ」
Ollama → LocalForge: 「イル」
...（リアルタイムで画面に表示）
```

技術的には **SSE（Server-Sent Events）** という仕組みを使っています。これはサーバーからブラウザへ一方向にデータを送り続ける技術です。

### キャッシュ（同じ質問への高速応答）

**キャッシュ（Cache）とは:** 一度計算した結果を保存しておき、同じ質問が来たら再計算せずに保存した結果を返す仕組みです。

**たとえ話:** 計算問題を解いて答えをメモ帳に書いておき、同じ問題が出たらメモを見るだけにする、というイメージです。

LocalForgeはQ&A結果を `.localforge/cache/responses/` に保存します。同じ質問（＋同じピン留め＋同じインデックス状態）なら、AIを呼ばずに即座に回答します。

---

## 8. コード生成（Generate）の仕組み

新しいプロジェクトを作る流れを説明します。

### 8-1. プラン生成

```
ユーザーが説明を入力
「FlaskとSQLiteを使ったTodoアプリを作ってください」
         │
         ▼
AIにプラン（設計図）を生成依頼
         │
         ▼
AIが返すプランの例:
┌─────────────────────────────────────┐
│ files:                              │
│   - path: app.py                    │
│     description: メインサーバー     │
│   - path: models.py                 │
│     description: データモデル       │
│   - path: templates/index.html      │
│     description: フロントエンド     │
└─────────────────────────────────────┘
         │
         ▼
ユーザーがプランを確認・承認
```

### 8-2. ファイル生成

プランが承認されると、ファイルを1つずつ生成します。

```
プラン承認
    │
    ▼
For each ファイル:
    │
    ├── プロンプト構築
    │   （要件 + 既存生成済みファイル + コンテキスト）
    │
    ├── Ollamaに生成依頼
    │
    ├── 生成されたコードを受信
    │
    └── ファイルをディスクに書き込み
         │
         ▼
全ファイル完了 → Gitコミット（自動）
```

**なぜ既存ファイルをコンテキストに入れるか?**
後から生成するファイルが、すでに生成されたファイルと整合性が取れるようにするためです。

**たとえ話:** チームで本を書くとき、後から書くメンバーは先に書かれた章を読んでから自分の章を書きます。

### 8-3. 途中再開（Resume）機能

生成が途中で止まった場合（エラー、キャンセルなど）、最初からやり直す必要はありません。

```
.localforge/generation_log.jsonl の内容:
{"file": "app.py",     "status": "completed"}  ← 完了
{"file": "models.py",  "status": "completed"}  ← 完了
{"file": "utils.py",   "status": "pending"}    ← 未完了（ここから再開！）
{"file": "index.html", "status": "pending"}    ← 未完了
```

**「続きから再開」ボタン:** 未完了のファイルだけを生成します。

**「最初から生成」ボタン:** 全ファイルを最初から生成し直します。

---

## 9. レポート生成（Explain）の仕組み

既存のコードベースを分析して、詳細なレポートを生成します。

### 9-1. 11セクションのレポート

LocalForgeは以下の11種類の分析を行います：

```
┌──────────────────────────────────────────────────┐
│  レポートの11セクション                          │
├────┬─────────────────────────────────────────────┤
│  1 │ Project Overview       プロジェクト概要      │
│  2 │ Module Map             モジュール構成        │
│  3 │ Entry Points           エントリーポイント    │
│  4 │ Data Flow              データフロー          │
│  5 │ API / Interface        API・インターフェース │
│  6 │ Data Models            データモデル          │
│  7 │ Business Logic         ビジネスロジック      │
│  8 │ External Dependencies  外部依存              │
│  9 │ Configuration          設定管理              │
│ 10 │ Error Handling         エラー処理            │
│ 11 │ Testing Strategy       テスト戦略            │
└────┴─────────────────────────────────────────────┘
```

### 9-2. レポート生成の流れ

```
レポート生成開始
    │
    ▼
インデックスから全ファイルのサマリーを取得
    │
    ▼
For each セクション（1〜11）:
    │
    ├── そのセクション用のプロンプトを作成
    │
    ├── Ollamaに分析依頼
    │
    └── 結果をリアルタイムで画面に表示
         │
         ▼
全11セクション完了
    │
    ▼
.localforge/report.md に保存
```

### 9-3. 途中再開機能

レポート生成も途中で止まった場合、完了したセクションはスキップして続きから生成できます。

---

## 10. その他の重要な仕組み

### 10-1. モデルのウォームアップ（事前ロード）

AIモデルは最初に使うときにRAMに読み込む必要があります。この読み込みには数秒〜数分かかります。

LocalForgeは、プロンプトを構築している間（Phase A〜G）に、**バックグラウンドで並行してモデルを読み込み始めます**。こうすることで、AIへの質問送信の準備が整う頃にはモデルもほぼ準備完了になります。

**たとえ話:** オーブンの予熱です。料理の準備をしながら同時にオーブンを温めておくことで、全体の時間を短縮できます。

```
時間軸:
0秒  ──────────────────────────────────────────► 時間

       モデルウォームアップ開始（バックグラウンド）
       ├──────────────────────────────────────────┤
       
       Phase A（ピン留め解決）
       ├──────────┤
                   Phase B〜E（検索・展開）
                   ├────────────────┤
                                    Phase F〜G（読み込み・構築）
                                    ├────────┤
                                             AIに質問送信
                                             ↑
                                         モデル準備完了（待ち時間ゼロ！）
```

### 10-2. トークン予算管理

AIには一度に渡せる情報量の上限（コンテキストウィンドウ）があります。LocalForgeはこの上限を超えないよう、**優先順位を付けて情報を選別します**。

```
トークン予算（例: 131072 トークン）の使い方:

プロジェクト概要      ←── まず確保
依存関係マップ        ←── 次に確保
サマリー              ←── 次に確保
会話履歴              ←── 次に確保
ピン留めファイル内容  ←── 優先的に確保
その他ファイル内容    ←── 残り予算で確保（予算切れなら打ち切り）
```

### 10-3. Gitとの連携

**Git（ジット）とは:** ファイルの変更履歴を管理するシステムです。

**たとえ話:** Wordの「変更履歴を記録する」機能を、プロジェクト全体に適用したものです。「いつ誰が何を変えたか」を全部記録できます。

LocalForgeは：
- コード生成後に自動でGitコミット（変更を記録）
- 新しいブランチで生成するオプション（本番を汚さない）
- ブランチの作成・切り替えもUI上で可能

**ブランチとは:** 開発の「分岐」です。実験的な変更を本流に影響させずに試せます。

### 10-4. OllamaのVRAM管理

**VRAM（ビデオRAM）とは:** グラフィックカード（GPU）のメモリです。AIモデルはGPUを使うと高速に動きます。

LocalForgeは使わなくなったモデルを自動でVRAMから解放します（`keep_alive: 0` をOllamaに送信）。これにより：
- 別のモデルに切り替えたとき、前のモデルが解放される
- アプリ終了時に自動でOllamaも終了する

### 10-5. セキュリティの仕組み

LocalForgeは以下のセキュリティ対策を取っています：

| 対策 | 内容 |
|------|------|
| **パス検証** | `../../../etc/passwd` のような危険なパスへのアクセスをブロック |
| **SQLインジェクション対策** | SQLを使っていないので該当なし |
| **XSS対策** | AI生成のHTMLはDOMPurifyでサニタイズ（危険なスクリプトを除去）|
| **ローカル限定** | サーバーは127.0.0.1のみ（外部からアクセス不可）|
| **シェルインジェクション対策** | コマンド実行は文字列ではなくリストで渡す |

---

## 11. 全体のデータフロー図

### フォルダ開設からQ&Aまでの完全な流れ

```
╔══════════════════════════════════════════════════════════════╗
║                    LocalForge 完全フロー                     ║
╚══════════════════════════════════════════════════════════════╝

    ユーザー操作
         │
         ▼
┌────────────────┐
│ フォルダを選択  │
└───────┬────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────┐
│                   フォルダ開設フェーズ                      │
│                                                            │
│  全ファイルをスキャン → ファイルツリー構築 → 画面表示      │
│  設定ファイル読み込み → モード判定 → UI更新                │
└────────────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────┐
│                  インデックス構築フェーズ                   │
│                                                            │
│  ファイル一覧取得                                          │
│       │                                                    │
│       ├── 変更なし → スキップ                              │
│       └── 変更あり → 読み込み → 種類判定                  │
│                           │                               │
│                    ┌──────┴──────┐                         │
│                    │             │                         │
│               シンプル         複雑                        │
│                    │             │                         │
│              自動要約    Ollama に要約依頼                  │
│                    │             │                         │
│                    └──────┬──────┘                         │
│                           │                               │
│                   index.jsonl に保存                       │
│                           │                               │
│                   Embedding 生成 → ChromaDB に保存         │
└────────────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────┐
│                      Q&A フェーズ                          │
│                                                            │
│  ユーザーが質問入力                                        │
│       │                                                    │
│  Phase A: ピン留めファイル解決（BFS 10段階展開）           │
│       │                                                    │
│  Phase B: ワークスペース検索                               │
│       │                                                    │
│  Phase C/D: セマンティック検索（ChromaDB or BM25）        │
│       │                                                    │
│  Phase E: 依存関係展開（BFS 最大200ファイル）             │
│       │                                                    │
│  Phase F: ファイル内容読み込み（深さ別文字数制限）         │
│       │                                                    │
│  Phase G: プロンプト構築（優先度・予算管理）               │
│       │                                                    │
│  Ollama に送信                                             │
│       │                                                    │
│  トークン受信（ストリーミング）→ 画面にリアルタイム表示   │
│       │                                                    │
│  回答キャッシュに保存                                      │
└────────────────────────────────────────────────────────────┘
```

### コード生成の流れ

```
╔══════════════════════════════════════════════════════════════╗
║                  コード生成（Generate）フロー               ║
╚══════════════════════════════════════════════════════════════╝

ユーザーが説明入力
    │
    ▼
Ollama にプラン生成依頼
    │
    ▼
プラン（ファイルリスト）を表示
    │
    ▼
ユーザーが確認・承認
    │
    ▼
ループ: 各ファイルを順番に生成
    │
    ├── プロンプト作成（既存ファイルをコンテキストに含む）
    ├── Ollama に生成依頼
    ├── コードを受信・保存
    └── generation_log.jsonl に「完了」を記録
         │
         ▼
全ファイル完了 → Git コミット
```

---

## まとめ

LocalForgeの裏側では非常に多くの処理が行われています。初心者向けにまとめると：

| 機能 | 裏側でやっていること |
|------|---------------------|
| フォルダを開く | ファイル一覧を取得・設定読み込み |
| インデックス構築 | 各ファイルをAIで要約・数値化して保存 |
| Q&A | 関連ファイルを検索→AIに渡して回答生成 |
| コード生成 | AIに設計図を作らせ→ファイルを1つずつ生成 |
| レポート | 11種類の視点でコードを分析 |

重要なキーワードの振り返り：

- **インデックス** = ファイルの目次・索引
- **Embedding** = テキストを数字（座標）に変換する技術
- **ベクトル検索** = 意味の近さでファイルを検索
- **RAG** = 検索して取得した情報をAIに渡して回答させる手法
- **BFS** = ファイルのつながりを広く探索するアルゴリズム
- **ストリーミング** = AIの回答をリアルタイムで1文字ずつ受け取る
- **キャッシュ** = 計算結果を保存して次回は高速化
- **トークン** = AIが処理する文字の単位
- **プロンプト** = AIに渡す質問＋背景情報の全体

---

*このドキュメントは LocalForge の実装コードから自動分析・生成されました。*
*生成日: 2026-05-22*
